# Catch Train (Android)

Android WebView 기반 SRT 좌석 감시 앱. 사용자가 SRT 사이트에서 직접 조회한 결과 표에서
**원하는 칸을 체크해 두면**, 그 칸이 열릴 때 알리고 **그 칸의 [예약하기] 버튼까지** 눌러 준다.

```
사이트에서 직접 조회 (사용자)
  → 결과 표를 DOM 으로 읽어 [열차 선택] 목록 생성
  → (열차 × 좌석등급) 칸 체크
  → 감시 시작: [조회하기] 실제 터치 → DOM 분석 → 체크한 칸이 예약가능?
  → 알림 → 그 칸의 [예약하기] 터치 → 여기서 끝
```

**좌석 선택과 결제는 사용자가 직접 한다.** 자동 결제·CAPTCHA 우회·로그인 자동화는
구현하지 않는다. [예약하기] 자동 클릭도 설정에서 끌 수 있고, 끄면 알림까지만 한다.

조회 조건은 앱이 갖고 있지 않다. 구간/날짜/시간은 사이트에 있는 그대로 쓴다.

---

## 문서

| 문서 | 내용 |
|---|---|
| [`CLAUDE.md`](CLAUDE.md) | **대원칙 · 코드 지도 · 작업 지침.** 코드를 건드리기 전에 읽는다 |
| [`docs/DESIGN.md`](docs/DESIGN.md) | 설계 규칙과 실측 메모. 코드 KDoc 이 `§번호` 로 참조한다 |
| [`docs/RELEASE.md`](docs/RELEASE.md) | 서명 키 / 버전 올리기 / APK 배포 |
| [`docs/HISTORY.md`](docs/HISTORY.md) | 원설계 의도, 폐기된 모델, 로드맵 (아카이브) |
| [`INSTALL.md`](INSTALL.md) | 최종 사용자에게 APK 와 함께 주는 설치 안내문 |

## 빌드

| 항목 | 값 |
|---|---|
| Android Studio | Ladybug (2024.2) 이상 |
| JDK | 17 |
| Kotlin / AGP | 2.0.21 / 8.7.3 |
| compileSdk / targetSdk / minSdk | 35 / 35 / 26 (Android 8.0) |
| UI | Jetpack Compose (Material 3) |

`minSdk 26` 인 이유: `java.time` 과 `NotificationChannel` 을 desugaring 없이 쓴다.

```bash
./gradlew --offline :app:assembleDebug
```

```bash
./gradlew --offline :app:testDebugUnitTest
```

release 빌드는 `--offline` 이 안 된다 → [`docs/RELEASE.md`](docs/RELEASE.md)

Android Studio 로 열면 (`Open` → 이 폴더) Gradle sync 가 필요한 배포판을 알아서 받아온다.

## 쓰는 흐름

1. 앱 실행 → WebView 가 SRT 메인(`https://etk.srail.kr/main.do`)을 표시
2. **WebView 안에서 직접 로그인**하고, 원하는 구간/날짜/시간으로 **조회**한다
3. `열차 선택` 패널의 `다시 읽기` → 지금 화면에 그려진 조회 결과를 그대로 읽어온다
   (조회 요청을 보내지 않고 DOM 만 읽으므로 몇 번을 눌러도 차단 위험이 없다)
4. 원하는 칸을 체크한다. 좌석 열은 **특실이 왼쪽, 일반실이 오른쪽** (사이트와 동일)
   - **매진인 칸도 체크할 수 있다.** 풀리기를 기다리는 것이 이 앱의 목적이다
   - 상태를 못 읽은 칸(`-`)만 체크할 수 없다
5. `감시 시작` — 첫 사이클은 현재 화면을 바로 분석하고, 이후 설정 범위 안에서
   **매번 무작위로 뽑은 간격**마다 재조회 → 분석 → 판정
   - 감시 도중에 체크를 바꿔도 된다. 재시작 없이 다음 사이클부터 반영된다
6. 체크한 칸이 `예약가능` 이 되면 알림 → [예약하기] 클릭 → `RESERVED`
   - `예약대기` 는 발견으로 보지 않는다 (즉시 예약이 아니라 대기 신청)
   - 누르는 사이 남이 먼저 잡아 `잔여석없음` 이 뜨면 **뒤로 가기로** 되돌아가 감시를 계속한다
7. 알림을 누르면 앱으로 복귀 → 좌석 선택과 결제를 직접 진행

선택은 **저장하지 않는다.** 그 조회 결과 화면에만 의미가 있는 값이라 앱을 다시 켜면 비어 있다.

## 제한 사항

- **화면이 켜져 있고 앱이 foreground 일 때만** 감시한다. 홈으로 나가면 일시정지된다.
  감시 중에는 화면이 꺼지지 않게 유지한다 (예약 화면에서도).
- 재조회 간격을 짧게 두면 요청이 그만큼 많아지고 **SRT 쪽에서 접속이 차단된다.**
  사이트 이용정책과 요청 제한을 준수할 것.
- 로그인/본인확인/결제는 자동화하지 않는다. 세션이 만료되면 감시를 멈추고 알린다.
- 열차 식별 기준은 **출발 시각**이다. 열차 번호는 파서가 읽지 못하는 경우가 있어
  보조 확인용으로만 쓴다.
- DOM selector 는 실제 사이트 구조에 맞춰야 한다. 파싱이 깨지면 고칠 곳은
  `webview/SrtSelectors.kt` 와 `webview/SrtParserScript.kt` 둘뿐이다
  → [`docs/DESIGN.md` §13](docs/DESIGN.md)
